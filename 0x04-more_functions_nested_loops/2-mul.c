#include"holberton.h"
/**
 * mul - multiplies two integers
 *@a: is the argument an integer
 *@b: is the argument of the function
 *Return: result
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
